# For RSS Feed Parsing
feedparser>=6.0

# For Sentiment Analysis Model
transformers>=4.0 # Use a version compatible with your fine_tuned_3class_model
torch>=1.8 # Or tensorflow if your transformer uses TF backend
accelerate>=0.12 # Often required by transformers for efficient loading

# For Price Prediction Model
tensorflow>=2.8 # Or the specific version your .h5 model was trained with
joblib>=1.0 # For loading the scaler
numpy>=1.19

# For yfinance Data Fetching (Test/Fallback)
yfinance>=0.2
pandas>=1.3

# Other necessary libraries (implicitly required by above, but good to list)
# sentencepiece # Often needed by tokenizers
# protobuf # Often needed by tensorflow/transformers 